// const country = "Poland";
// const continent = "Europe";
// let polandPopulation = 40;
// const language = "Polish";
// isIsland = false;

// polandPopulation = 38;

// console.log(country, continent, polandPopulation, isIsland, language);
// console.log(typeof country, typeof continent, typeof polandPopulation, typeof isIsland, typeof language);

// polandPopulation++

// let halfOfPoland = polandPopulation / 2;
// let firstHalfOfPoland = halfOfPoland;
// let secondHalfOfPoland = halfOfPoland;

// console.log(firstHalfOfPoland, secondHalfOfPoland);

// let finlandPopulation = 6;

// console.log(finlandPopulation < polandPopulation);

// let averagePopulationOfAllCountries = 33;

// console.log(polandPopulation > averagePopulationOfAllCountries);

// description1 = (country + " is in " + continent + " and its " + polandPopulation + " million speak " + language);
// console.log(description1);

// desceiption2 = `${country} is in ${continent} and its ${polandPopulation} million speak ${language}.`;
// console.log(desceiption2);

// numNeighbours = Number(prompt(`How many neighbour countries does your country have`));

// if (numNeighbours === 1) {
//     console.log(`Only 1 border`);
// } else if (numNeighbours > 1) {
//     console.log(`More than one neighbour`);
// } else {
//     console.log(`No borders`);
// }

const country = `Canada`;
const population = 33;
const language = `English`;
isIsland = false;

// if (language == `English` && population < 50 && !isIsland) {
//     console.log(`You should live in ${country}`);
// } else {
//     console.log(`${country} does not meet your criteria`);
// }


// const language = prompt(`Type language`);

// switch (language) {
//     case `chinese`:
//         console.log(`MOST number of native speakers!`);
//         break;
//     case `spanish`:
//         console.log(`2nd place in number of native speakers`);
//         break;
//     case 'english':
//         console.log(`3rd place`);
//         break;
//     case `hindi`:
//         console.log(`Number 4`);
//         break;
//     case `arabic`:
//         console.log(`5th most spoken language`);
//         break;
//     default:
//         console.log(`Great language too :D`);
// }


// console.log(`${country}'s population is ${population > 33 ? `above` : `below`} average`);

const bill = 275;

/* Write your code below. Good luck! 🙂 */

const tip = 15 / bill;

console.log(`The bill was ${bill}, the tip was ${tip}, and the final value ${bill + tip}`);